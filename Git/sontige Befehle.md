* `git --no-pager log ...` - keinen Page bei der Ausgabe benutzen
* `git push --force remote-repo local-branch` - Pushen forcen
* `git rm --cached -r pfad/ datei.xyz` - Dateien untracken; `-r` - Rekurisiv für Dateien im Ordner
